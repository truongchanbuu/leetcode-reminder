{
  "name": "patternqueue",
  "version": "0.6.1",
  "private": true,
  "type": "module",
  "description": "A local-first LeetCode pattern roadmap and spaced-repetition extension.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test && npm run build",
    "package": "node scripts/package.mjs"
  },
  "dependencies": {
    "ts-fsrs": "^5.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.10",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
